# Reference

API/package reference will be generated from Go doc comments once code exists (Phase 0 onward). MCP tool reference (exact tool names, input/output schemas) lands with Phase 7 of [`PLAN.md`](../../PLAN.md), once `internal/mcp/server` is implemented. Nothing to reference yet.
